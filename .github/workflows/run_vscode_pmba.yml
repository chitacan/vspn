name: run vscode on pmba
on:
  workflow_dispatch:
    inputs:
      uri:
        description: uri to open with vscode

jobs:
  run_vscode:
    runs-on: [self-hosted, pmba]
    steps:
      - run: code -n --folder-uri ${{ github.event.inputs.uri }}
        env:
          RUNNER_TRACKING_ID: "" 
